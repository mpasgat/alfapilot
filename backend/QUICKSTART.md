# 🚀 Quick Start Guide - Alfapilot Backend

## ✅ Шаг 1: Получение API ключа OpenRouter

1. Откройте https://openrouter.ai
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "Keys" или "API Keys"
4. Создайте новый ключ (бесплатно)
5. Скопируйте ключ (начинается с `sk-or-v1-...`)

## ✅ Шаг 2: Настройка окружения

1. Откройте файл `.env` в корне проекта
2. Найдите строку:
   ```
   OPENROUTER_API_KEY=<YOUR_OPENROUTER_API_KEY>
   ```
3. Замените `<YOUR_OPENROUTER_API_KEY>` на ваш настоящий ключ:
   ```
   OPENROUTER_API_KEY=sk-or-v1-your-actual-key-here
   ```
4. Сохраните файл

## ✅ Шаг 3: Запуск Backend

### Вариант A: Через батник (Windows)

1. Откройте папку `backend`
2. Двойной клик на `start_backend.bat`
3. Дождитесь сообщения "Uvicorn running on http://0.0.0.0:8000"

### Вариант B: Через PowerShell

```powershell
cd backend
python app\main.py
```

### Вариант C: Через Docker

```powershell
cd backend
docker build -t alfapilot-backend .
docker run -p 8000:8000 -e OPENROUTER_API_KEY=your_key alfapilot-backend
```

## ✅ Шаг 4: Проверка работы

Откройте в браузере:

- **API Документация**: http://localhost:8000/docs
- **Health Check**: http://localhost:8000/health
- **Альтернативная документация**: http://localhost:8000/redoc

## ✅ Шаг 5: Тестирование

Запустите тесты (после запуска сервера):

```powershell
cd backend
python test_api.py
```

## 🎯 Быстрый тест через Swagger UI

1. Откройте http://localhost:8000/docs
2. Найдите `/api/v1/marketing/generate-posts`
3. Нажмите "Try it out"
4. Введите данные:
   ```json
   {
     "idea": "Запуск нового AI продукта",
     "tone": "professional",
     "target_audience": "стартапы"
   }
   ```
5. Нажмите "Execute"
6. Проверьте ответ с вариантами постов!

## 🐛 Решение проблем

### Ошибка: "ModuleNotFoundError: No module named 'app'"

**Решение:**
```powershell
cd backend
pip install -r requirements.txt
```

### Ошибка: "OPENROUTER_API_KEY is not set"

**Решение:**
- Проверьте файл `.env`
- Убедитесь, что ключ указан без скобок `<>`
- Убедитесь, что файл `.env` находится в корне проекта (не в папке backend)

### Порт 8000 занят

**Решение 1 - Изменить порт:**

Отредактируйте `backend/app/main.py`, последнюю строку:
```python
uvicorn.run(app, host="0.0.0.0", port=8001)  # Используйте порт 8001
```

**Решение 2 - Освободить порт:**
```powershell
# Найти процесс
netstat -ano | findstr :8000

# Завершить процесс (замените PID)
taskkill /PID <PID> /F
```

### Медленные ответы от API

- Первый запрос может быть медленным (холодный старт модели)
- Проверьте интернет-соединение
- Убедитесь, что OpenRouter работает: https://status.openrouter.ai

## 📚 Дальнейшие шаги

1. ✅ Запустите backend
2. ✅ Протестируйте через Swagger UI
3. ✅ Запустите тесты `test_api.py`
4. 🤖 Настройте и запустите Telegram бота
5. 🔗 Интегрируйте бота с backend

## 🆘 Поддержка

Если проблема не решена:
1. Проверьте все шаги еще раз
2. Посмотрите логи в терминале
3. Откройте issue на GitHub

---

**Готово!** Backend должен работать на http://localhost:8000 🎉
